version: '3.8'

services:
  app:
    container_name: tictactoe-multiplayer
    image: fredrikjern/tictactoe-multiplayer:0.0.14
    restart: unless-stopped
    build: .
    ports:
      - "3012:3000"
    environment:
      - MONGO_URI=mongodb://root:test@tictactoe_db:27017/
    depends_on:
      - tictactoe_db

  tictactoe_db:
    container_name: tictactoe-multiplayer-db
    image: mongo:6.0
    restart: unless-stopped
    environment:
      - MONGO_INITDB_ROOT_USERNAME=root
      - MONGO_INITDB_ROOT_PASSWORD=test
    volumes:
      - tictactoe_db:/data/db

volumes:
  tictactoe_db: